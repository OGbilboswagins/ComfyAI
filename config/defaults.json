{
  "version": 1,

  "defaults": {
    "chat": "local_chat",
    "plan": "local_plan",
    "build": "cloud_build"
  },

  "workflow": {
    "rewrite": {
      "use_cloud_llm": true,
      "max_tokens": 4096
    }
  },

  "mcp": {
    "enable": false,
    "endpoint": null
  }
}
